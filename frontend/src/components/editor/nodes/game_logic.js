import { useNodesStore } from "./node_store"
import { ref } from "vue"


const output = ref("TEST")
const gameNodes = new Map()


const compileGame = () => { //Gets all nodes and puts them in a more readable form, removing unnecessary information specifically relating to nodes as they are in vueflow

  }

  const start = (compiledGame) =>{

  }
  



export default{
      start,
      compileGame,
      output,
}