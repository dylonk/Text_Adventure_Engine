{
  "name": "text-adventure-platform",
  "version": "1.0.0",
  "scripts": {
    "start:web": "cd frontend && npm run serve",
    "start:api": "cd backend && npm run start",
    "start:electron": "cd electron && npm run start",
    "build:all": "npm run build:web && npm run build:electron",
    "build:web": "cd frontend && npm run build",
    "build:electron": "cd electron && npm run build"
  }
}